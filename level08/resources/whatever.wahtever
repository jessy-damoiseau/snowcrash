on a un fichier token ou on a pas les perm de lecture et d'ecriture
et un fichier ./level08 qui apres decompilation nous indique qui open, read and write un file passe en parametre sauf si le file a token dans son name

on fait donc un lien symbolique vers le file token et on use ce lien pour l'open

ln -s $HOME/token /tmp/link && ./level08 /tmp/link 

ceci nous donne le pwd de flag08 : quif5eloekouj29ke0vouxean

depuis flag08 on peux faire un getflag et recup le flag de level09
