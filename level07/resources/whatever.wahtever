en decompilant ./level07 on vois que le programe recupere la var d'env LOGNAME et l'ecrie dans un asprintf avec un echo


il sufi donc de faire en sorte que echo exec getflag via LOGNAME

on ecrie donc: 

export LOGNAME='$(getflag)' && ./level07 

ce qui donne le flag 